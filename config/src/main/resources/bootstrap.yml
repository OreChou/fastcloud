server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/OreChou/fastcloud-config
          search-paths: hello-service
          username: OreChou
          password: Rangers1018
  application:
    name: config-server
